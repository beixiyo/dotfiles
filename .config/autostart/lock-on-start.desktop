# 配合 SDDM 窗口管理器 krdp 开机自启动
# sudo mkdir -p /etc/sddm.conf.d
# sudo tee /etc/sddm.conf.d/autologin.conf << 'EOF'
# [Autologin]
# User=yourname
# Session=plasma
# EOF

[Desktop Entry]
Type=Application
Name=Lock Screen on Start
X-KDE-autostart-after=panel
Exec=loginctl lock-session